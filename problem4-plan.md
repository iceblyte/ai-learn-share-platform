# problem4 修复计划

## 背景

`problem4.md` 指出 AI 自然语言搜索、AI 推荐理由、AI 个性化推荐列表不可用，AI 摘要偶发超时。现有数据库脚本在 `db/` 下，本次优先修复运行时逻辑和接口契约，不新增表结构。

## 诊断结论

1. `GET /api/v1/ai/recommendations` 后端返回 `List`，前端按 `PageData.records` 读取，导致推荐列表为空。
2. AI HTTP 调用使用默认 `RestTemplate`，没有显式连接/读取超时，外部服务慢响应会影响摘要、搜索解析和推荐理由。
3. 自然语言搜索过度依赖 AI JSON；当 AI 不可用、返回格式异常、返回标签/分类/评分过滤过严时，即使数据库有资源也可能返回 0 条。
4. 推荐理由依赖 AI 文本生成，失败后理由过于通用，用户感知为 AI 功能不可用。

## 修复策略

1. 不使用 OpenSpec 和 git；本次只修改源码、`db/` 变更记录和项目文档。
2. 后端增加 AI 调用超时与可配置生成参数，避免摘要接口长时间等待。
3. 自然语言搜索改为“AI 解析优先 + 本地规则解析兜底 + 空结果放宽过滤重试”。
4. 对“评分最高/最热/最新/前 N 个/Java 并发”等常见中文意图做本地确定性解析，涉及数据库查询时以本地校验结果为准，避免完全依赖模型输出。
5. 推荐接口改为分页响应，并兼容前端现有 `page/size` 调用。
6. 推荐理由失败时使用本地兴趣、标签、评分和热度生成可解释理由。
7. 前端兼容旧/新推荐响应结构，并优化 AI 搜索无结果时的提示。
8. 更新 README、项目快照、AI 开发日志和测试文档。

## 验证清单

1. 后端编译或测试通过：当前 WSL 环境未安装 Java，`mvn test` 无法启动；代码已完成静态检查和前端联调类型修复。
2. 前端构建通过：`npm run build` 已通过。
3. 自然语言查询“推荐关于Java并发编程且评分最高的前5个资源”可解析出关键词、排序和数量，并在 AI 失败时仍查询数据库。
4. 首页“为你推荐”能读取分页推荐列表，并兼容旧数组结构。
5. 推荐理由接口在 AI 失败时仍返回每个资源对应理由。

## 执行顺序

1. 更新本计划文档。
2. 修复后端 AI 搜索与推荐接口。
3. 修复前端推荐读取和搜索空状态。
4. 补充 `db/` AI 配置变更记录。
5. 更新 README、项目快照、AI 开发日志和测试文档。
6. 运行后端测试/编译和前端构建验证。
