- # 项目目标：AI个性化学习资源分享平台端到端开发 (Project Master Goal)
  
  ## 1. 角色与任务说明
  你是一个顶级全栈 AI 工程师及产品交付团队。你的任务是根据 OpenSpec 规范，在 Windows (PowerShell 7) 环境下，从零开始完整构建本平台。
  请调用 `sperpowers` 和 `ui-ux-pro-max` 技能（skills），严格按照底部的【执行工作流】一步步完成文档输出、代码编写和测试。
  
  ## 2. 项目背景
  构建一个面向大学生的学习资源共享社区。用户可以上传、浏览笔记、视频、电子书等资源。平台需根据用户兴趣标签和历史行为，利用大模型（兼容 Gemini API）和推荐算法实现内容推荐及智能搜索。
  
  ## 3. 技术栈要求
  - **前端**：Vue 3 + TypeScript + Tailwind CSS
  - **后端**：Spring Boot (Java)
  - **数据库**：MySQL + Redis
  - **AI 对接**：Spring AI (Google GenAI Starter)，支持多模型切换
  - **测试**：基于 Python+Pytest/Postman 的接口测试，Playwright/Selenium 的 UI 测试
  
  ## 4. 核心功能需求
  1. **用户与权限管理**：
     - 角色：普通用户、资源发布者、管理员。
     - 鉴权：JWT 认证，RBAC 权限控制。
  2. **资源管理 (CRUD)**：
     - 支持发布资源（标题、分类、自定义多标签、详细描述、文件上传或外部链接）。
  3. **搜索与筛选模块**：
     - 全文检索（基于 MySQL），支持按分类、多标签、评分、热度等组合过滤。
  4. **AI 增强功能（核心）**：
     - **AI智能摘要**：后端对接 LLM，自动生成约100字的精准摘要。
     - **NL2API/NL2SQL**：自然语言搜索（如“推荐关于Java并发且评分最高的前5个资源”），AI 解析为结构化查询。
     - **个性化推荐**：结合标签/协同过滤推荐资源，并调用 AI 生成个性化“推荐理由文案”。
  5. **社区互动**：
     - 点赞、收藏、评分、多级评论。
  
  ## 5. 执行工作流 (Action Plan)
  由于项目庞大，请严格按照以下顺序执行。**在完成每一个大步骤并输出相应文件后，简要向我汇报，并主动请求继续下一步**：
  
  - **Step 1：产品与架构设计 (写入 `openspec/` 目录)**
    - 生成 `proposal.md`：包含 PRD 背景、用户画像、核心业务流程（使用 Mermaid 流程图）、需求原型描述。
    - 生成 `design.md`：必须包含系统架构图选型说明、数据库建表结构说明及 Mermaid ER图、符合 RESTful 规范的 API 接口设计文档。
    
  - **Step 2：任务拆解 (写入 `openspec/` 目录)**
    - 基于 Design 生成 `tasks.md`，将前后端及测试的开发任务拆解为细粒度的 TODO 列表。
  
  - **Step 3：脚手架搭建与环境配置**
    - 使用 PowerShell 7 兼容的命令初始化前后端工程目录树结构。
    - 编写根目录 `README.md`（含项目介绍、环境依赖、启动配置教程）。
    - 生成数据库建表 SQL 脚本（含索引和注释），保存在 `db/` 目录下。
  
  - **Step 4：迭代编码实现**
    - 优先开发后端 Spring Boot 核心 CRUD 与 JWT 鉴权。
    - 集成 AI 模块（摘要、NL2SQL、推荐文案）。
    - 开发前端 Vue 3 项目，完成 API 对接与 UI 渲染。
  
  - **Step 5：测试与验收交付**
    - 编写测试用例表，并产出对应的接口自动化脚本思路/代码和 UI 自动化核心脚本。
    - 生成测试报告模板。
    - 生成 `AI_Dev_Log.md`：记录开发链路中使用的提示词、MCP/Skills 调用的逻辑等 AI 辅助信息。
