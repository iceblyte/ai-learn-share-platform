-- AI 运行时修复记录 - 2026-05-19
-- 本次不修改表结构和数据，仅记录新增运行时配置项。
--
-- 新增 application.yml / 环境变量配置：
--   AI_CONNECT_TIMEOUT_MS  默认 3000
--   AI_READ_TIMEOUT_MS     默认 8000
--   AI_TEMPERATURE         默认 0.2
--   AI_MAX_TOKENS          默认 512
--
-- 目的：
--   1. 避免 AI 摘要、自然语言搜索解析、推荐理由生成在外部服务慢响应时长时间阻塞。
--   2. AI_API_KEY 未配置或仍为占位值时快速降级，不影响数据库搜索和推荐列表。
--   3. 自然语言搜索涉及数据库查询时使用后端本地规则做确定性校验和空结果放宽重试。
