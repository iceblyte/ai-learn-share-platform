目前存在的问题：

1. “AI 自然语言搜索”功能中无法正常使用，比如我输入“推荐关于Java并发编程且评分最高的前5个资源”，然后我明明把Redis缓存清空了，数据库中也有相关的数据，但是就是显示暂无搜索结果，是不是ai服务有问题？（“AI 智能摘要”功能倒是可以使用，但是有时候会显示超时。），此外，“AI 推荐理由”和“AI个性化推荐列表”等AI相关服务都无法使用。可以考虑使用 springai 的 function calling，比如在需求中包含严格的逻辑校验或需要读写数据库时。
2. 原有的数据库的表结构和数据位于 db 文件夹下，如果要修改对应的表结构或者数据的话，请在此文件夹下创建对应的 .sql 文件。

现在，你要修复上述所有功能，注意使用 superpowers、ui-ux-pro-max，先写一个 plan 的 md 文档，再根据 plan 修复以上问题。

不用 openspec 和 git。

全部bug修复好后，更新本项目的所有对应文档。